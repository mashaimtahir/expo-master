{
  "name": "solution",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && node server.js && cd ../client && npm start",
    "build": "cd client && npm install && npm run build && cp -r public ../ && cd ../ && echo /'Build is ready to deploy /' && ls public/**/*"
  },
  "author": "",
  "license": "ISC"
}
